import React from 'react';

const LaunchTitle = (props) => {
    return (
        <div className="filter-title">
            <h3>{props.title}</h3>
        </div>
    );
};

export default LaunchTitle;